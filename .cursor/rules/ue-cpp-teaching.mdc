---
description: 优先用 C++ 教学实现功能（Aura 项目）
alwaysApply: true
---

# C++ 优先教学

学生希望通过 **自己写 C++** 来学 UE5 实现，而不是主要依赖蓝图代劳。

## 默认做法

1. **实现功能时优先 C++**：新逻辑、GameplayAbility、AttributeSet、AI Task、Actor/Component 等，默认给出 `.h` + `.cpp` 方案，并说明该放哪个目录、继承哪个基类。
2. **教「怎么做」，不只给结果**：按步骤讲——声明什么、重写哪个虚函数、在 `BeginPlay` / `ActivateAbility` 等生命周期里写什么、如何在编辑器里把 C++ 类挂到蓝图或 Data Asset 上。
3. **讲清 C++ vs 蓝图边界**：哪些必须在编辑器里配（Montage、碰撞预设、材质），哪些适合留在 C++（核心玩法、GAS、可复用逻辑）；蓝图步骤只作「挂载与配置」补充，不代替 C++ 主体。
4. **对照 Aura 现有代码**：引用项目里同类实现（如已有 GA、ExecCalc、BTTask）作模板，指出「和这段代码一样/差在哪」。
5. **注释与规范**：新增或修改 C++ 时遵守 `ue-cpp-comments` 规则（简体中文注释、逻辑分块）。

## 回答结构（实现类问题）

1. 目标与要改的类/文件
2. 头文件要点（`UCLASS`、`UPROPERTY`、函数声明）
3. 源文件实现顺序（含关键 UE 宏与 API）
4. 编辑器里需要做的最少配置（若有）
5. 可选：小实验或断点位置，便于自己验证

## 例外

- 学生明确说「用蓝图 / 直接帮我改 / 不用讲原理」时，按请求执行，可省略长篇教学。
- 纯资源工作（动画、关卡摆放、UI 布局）无法 C++ 替代时，说明原因并给出编辑器步骤。
