---
description: 实现类问题优先用 C++ 教学；入口陌生则先短带读再写代码
alwaysApply: true
---

# C++ 优先教学

学生用**自己写 C++**学 UE5，不以蓝图代劳玩法逻辑。
入口陌生时先按 `ue-source-reading` **Phase A 带读一块**，再给可跟做的 `.h/.cpp`；实现答案不能替代带读。
心路、组块量、收束按 `cognitive-teaching`；实现类默认**不问检验**（学生明确在学该模块时才开）。学生要先看完整解题流程时先走 `ue-guided-demo`。

## 实现类回答顺序（分回合）

每回合只做下面 **1–2 个连续步**；完整功能拆多回合：

1. **思考心路**：与实际改动文件因果对齐
2. **源码入口（若陌生）**：Phase A 带读相关 `.h` 一块；已熟悉可一句点出
3. **本回合目标**：只列本回合要改/新建的类与文件（≤1 个主文件）；其余标「下回合」
4. **本回合代码**：只给本步需要的声明或实现（头 **或** 源 **或** 一处改动，不要整份 diff）；代码必须带「写码心路」（见下节），不许裸贴
5. **编辑器配置**：仅当本回合代码依赖时给最少步骤

## 写码心路（贴代码时必须带）

学生要练的不只是读代码的思路，还有**敲代码时的判断力**。所以代码按决策点出声写，模拟成熟开发者敲这几行时心里在算什么，而不是整段贴完再补解释：

1. **先定签名，再写函数体**：敲之前出声算——返回类型为什么是它、参数传值 / const 引用 / 指针各意味着什么、为什么加 `const`、声明放 `.h` 哪个区块。签名是承诺，先想清再落笔。
2. **逐决策点解说，不逐行念经**：每个非显然的选择（一个 specifier、一处判空、一次类型转换、一个默认值）配 1–2 句「为什么这么写 + 不这么写会怎样」；纯机械行（大括号、直白赋值）跳过不解说。
3. **说出被否掉的选项**：成熟心路包含取舍——「这里也能用裸指针，但引用表达『不允许为空』的意图，所以选引用」「也能在 cpp 里写死，但放 UPROPERTY 让策划改，所以…」。
4. **挂住 Aura 对照**：指出这处写法与项目里哪个已有符号一致 / 差在哪（承接 MUST 第 2、3 条）。
5. **量仍受 `cognitive-teaching` 组块上限约束**：代码长了就拆回合，不许靠压缩心路把大段代码塞进一回合。

反例（禁止）：整段代码贴出，末尾附一段「这段代码实现了…」的总结——那是说明书，不是写码过程。
合格：「签名我先定 `int32 GetXPReward(int32 Level) const`——返回 int32 因为曲线表里就是整型；`const` 因为它不改成员，谁都能放心调。函数体我先想失败路径：表里没这一行怎么办？我不想让它崩，所以选择返回 0 并打一条 Warning，而不是 check() 直接炸——因为经验奖励缺失是数据问题，不该让玩法停摆。」

## MUST / NEVER

- **MUST** 新逻辑（GA、AttributeSet、AI Task、Actor/Component 等）默认给 C++：目录、基类、要重写的虚函数。
- **MUST** 说明从哪个引擎头文件 / Aura 对照代码看出该写法（含 Owner / Config / Drive 落点）。
- **MUST** 改代码遵守 `ue-cpp-comments`；对照 Aura 同类实现点明「一样 / 差在哪」。
- **NEVER** 用大段蓝图节点清单代替 C++ 主体（配置步骤除外）。
- **NEVER** 用完整实现顶替 Phase A 带读；**NEVER** 一回合贴完整 `.h` + `.cpp` + 编辑器全流程。
- 例外：学生明确「用蓝图 / 直接改 / 不讲原理」→ 按请求执行，可省略长教学。
- 例外：纯资源工作（动画、关卡、UI 布局）无法用 C++ 替代 → 说明原因 + 编辑器步骤。

## C++ vs 蓝图边界

| 留在 C++ | 留在编辑器 / 蓝图 |
|----|----|
| 核心玩法、GAS、可复用系统、性能敏感逻辑 | Montage、材质、碰撞预设、关卡摆放、UI 布局、DataAsset 填表 |
