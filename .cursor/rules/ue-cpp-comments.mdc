---
description: 编写或修改 Aura 的 .h/.cpp 时使用简体中文注释与逻辑分块
globs: "**/*.{h,cpp}"
alwaysApply: false
---

# UE C++ 中文注释

改 Aura 的 `.h` / `.cpp` 时必须用**简体中文**注释，解释**意图与原因**，禁止复述字面（勿写 `// 获取 Pawn` 对着 `GetPawn()`）。

## 层级

| 层级 | 格式 | 写什么 |
|------|------|--------|
| 类 / 结构体 | `/** ... */` 在 `UCLASS`/`USTRUCT` 上 | 职责、在系统中的位置 |
| 函数 | `/** ... */` 在声明上 | 功能、关键参数、返回值、副作用 |
| 成员 | `/** ... */` 或 `//` 在 `UPROPERTY` 上 | 用途与取值含义 |
| 函数体内 | `//` | **每个逻辑小块前**一行：这一步做什么、为什么 |

## 函数内分块（强制）

典型块：有效性检查 → 取组件/ASC/黑板数据 → 分支/循环意图 → 业务计算 → 写回/委托/应用 GE。  
主流程可用 `// 1.` `// 2.`（参考 `DamageExecutionCalculation.cpp`）。

```cpp
// ❌ 无分块、无意图
APawn* Pawn = OwnerComp.GetAIOwner()->GetPawn();
if (!Pawn) return;

// ✅
// AI 的 Pawn 无效则本帧不更新
APawn* Pawn = OwnerComp.GetAIOwner()->GetPawn();
if (!Pawn) return;
```

## 其他

- 新增/大改函数不得提交无注释大段实现；只改一两行时，所在逻辑块仍须有注释。
- 保留 UE 文件头；`GENERATED_BODY()` 上等宏上方不写多余注释。
- 用户说「少注释 / 只改一行」时可在该次放宽。
