---
description: 完整带做示范：从需求到验证走完全程，展示人类开发者解题的视角与思考顺序
alwaysApply: true
---

# 完整带做示范（Guided Walkthrough）

适用信号：「带着做一遍 / 完整流程 / 从头到尾演示 / 你遇到这类问题怎么想 / 先带我熟悉一遍再细学」。

目标：让学生先**看一遍完整解题过程**，建立「人类开发者从什么视角切入、按什么逻辑走完全程」的心智模型，再进入分块细读或自己动手。

与 `ue-source-reading` 的分工：带读回答「这个模块怎么用」（逐块读符号）；带做回答「这类问题怎么解」（全程走一遍）。学生不熟开发全流程时**带做优先**，模块细读后补。

## 选材

- 用 **Aura 里真实的小功能**做载体（如 Lab 里加一个测试技能、给一种敌人加攻击动作），不虚构示例。
- 体量：能在一个流程内从需求跑到可验证；不选需要多系统联动的大功能。

## 流程（全程心路外化）

按开发真实顺序推进；每步先写「这一步我在想什么、为什么看这里」，再动手：

1. **需求复述**：把需求翻成开发语言（谁触发、谁执行、结果是什么）
2. **从已知出发**：学生手上已有什么相关符号（自己写过的类 / 配过的资产）→ 对它问「谁调它 / 它调谁 / 谁创建它」（不靠猜，见 `cognitive-teaching`「出发点」）
3. **找同类**：Aura 里谁已经干过类似的事 → 搜符号、打开对照文件
4. **读基类**：要重写的虚函数从哪个引擎头文件看出来
5. **动手实现**：按 `ue-cpp-teaching` 分回合给代码
6. **编辑器挂载**：最少配置步骤
7. **验证**：预期看到什么 → 实际对比 → 不符则转入调试心路（假设 → 查哪层 → 最小验证）
8. **复盘**：一句话串起全程「需求 → 已知起点 → 引用链 → 基类 → 实现 → 验证」，并顺手提炼 1 条可复用通则

## 节奏

- 全程 **2–4 个回合**走完；每回合推进流程中的 **1–2 步**，组块量、检验、收束仍按 `cognitive-teaching`。
- 思考用**人类开发者第一人称**融在行文里——不写独立心路块，但推理必须**出声可见**（「我现在打开什么、为什么、预期看到什么、看到后的判断」），不许只在幕后推完再端结论。突出视角与取舍：为什么先看同类而不是先翻引擎文档、为什么从这个符号往这个方向追。每一步必须答**双重 why**（为什么这么走 / 为什么这么做，见 `cognitive-teaching`）——这是带做的核心价值，不许退化成「第一步干嘛、第二步干嘛」的步骤清单。
- 学生喊「太快 / 跟不上」→ 每回合缩到 1 步；喊「直接帮我做完」→ 转代劳，仍保留复盘一步。

## 收尾（必须）

示范走完后，给一个**同构小变体**让学生模仿独立完成（如「照这个思路给另一种敌人加同样的能力」），学生交卷后再点评，完整答案不放最前。这一步把「看过」变成「会走」。
