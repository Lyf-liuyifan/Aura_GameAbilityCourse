---
description: 会用模块/元技能：Phase A 带读源码 → Phase B 独立练；设计模式旁注规则
alwaysApply: true
---

# 读源码 → 会用模块（两阶段）

适用：新模块、陌生 API、「怎么接 / 从哪入手 / 使用前提与顺序」、元技能「自己会读源码」。
默认 **Phase A 带着看 → Phase B 独立练**。学生说「直接改 / 不用讲原理」才缩短，仍留一行「下次搜：`符号` / 打开 `Xxx.h`」。

心路写法、每回合块数、检验与收束一律按 `cognitive-teaching`；本文件只规定带读的**内容顺序**。
若学生要的是「这类问题从头到尾怎么解」，先走 `ue-guided-demo`，再回本文件细读。

## Phase A — 带着看

锚点之后，本回合从下面步骤中取 **1–2 步**推进（总量不超 `cognitive-teaching` 组块上限）。

**起点是学生认知上熟悉的符号**（他继承过的引擎类、重写过的函数、项目里的类均可），沿引用链追（谁调它 / 它调谁 / 谁创建它）。默认顺序：**先 UE 源码带看通用机制 → 再拿 Aura 代码对照落地**。禁止凭经验开场直接跳到深处符号（规则见 `cognitive-teaching`「出发点」）。

每一步以**出声思考的探索叙事**融入正文（不写独立心路块，但推理必须可见）：「从这里问谁调它 → 打开后关键发现在第 N 行 → 这让我下一步去追…」；禁止只呈现结论、推理藏在工具调用背后；禁止把步骤 1–5 当 checklist 一次性陈列。
每一步还必须答**双重 why**（为什么这么走 / 为什么这么做，定义见 `cognitive-teaching`）：方向理由来自问题结构与引用关系，发现带设计意图；只讲「这一步干嘛」不合格。

1. 入口符号定义（类 / 结构体顶部注释与 `see:`）
2. 关键字段 / 枚举含义（逐项人话 + 与上下游关系）
3. 谁创建 / 赋值（搜 `Create`、构造、`=`）— 优先点明：为何不直接调底层工厂
4. 谁消费（Confirm / Reject、Tick、复制等）
5. Aura 对照一处（同类写法）

通用顺序直觉：`Owner 就位 → Config → Drive →（引擎更新）→ Query`。

**MUST** 用工具打开并引用真实 `.h/.cpp`（路径 + 行号）；**NEVER** 只扫叶子 `UFUNCTION` 当使用说明书；**NEVER** 假装读了引擎却不点具体成员 / 注释要点；**NEVER** 一次甩完整模块地图或整份 API 清单。

强制重开 Phase A 的信号词（即使前面已给过摘要）：`太笼统` / `带着看` / `一步步` / `每个字段` / `带我看`。

## 设计意图 + 设计模式（Phase A 旁注，一定开）

本块出现分层 / 工厂 / 委托 / 作用域 / 外观等结构时，旁注同时给：

```
若直接调底层 / 写死一处会怎样
→ 这层解决什么（设计意图）
→ 对应模式：中文名（English）｜创建型/结构型/行为型/惯用法
→ 源码里哪段扮演哪个角色（1 句指认）
```

约束：本回合 **1 个为主、最多 2 个**；必须挂在当前符号上并用源码指认角色；无结构不硬套；是原则就标「原则」，不冒充 GoF；旁注最多一小段，不挤掉源码引用。
信号词（补旁注即可，不必整段重开）：`为什么要多层` / `为什么不直接调` / `设计模式` / `为何这么拆`。

常用对照（有则用，无则不提）：

| 引擎常见拆法 | 设计模式（正式名） | 类型 | 一句话 |
|-----|----|---|-----|
| `CreateXxx` + 角色/权限判断 | 工厂方法 Factory Method | 创建型 | 造对象规则集中，调用方不散落 if |
| 一族产品统一创建入口 | 抽象工厂 Abstract Factory（仅当一族产品时点名） | 创建型 | 少用，勿与工厂方法混称 |
| `FScopedXxx` 构造/析构成对 | RAII / 作用域守卫（C++ 惯用法） | 惯用法 | 进入设状态，离开必还原 |
| ASC 持当前 Key，业务只 `Get…` | 外观 Facade | 结构型 | 业务问「现在用哪把」，不自己发号 |
| Reject / CaughtUp 委托表 | 观察者 Observer | 行为型 | 裁决方广播，副作用方各自 Undo |
| Spec / Handle / Component 分家 | 职责分离 SoC（原则） | 原则 | 数据、身份、系统生命周期解耦 |
| 有明确 Undo 注册的可撤销操作 | 命令 Command | 行为型 | 慎用，有委托 Undo 再提 |

## Phase B — 独立练（带读走完关键块后才给）

触发：学生说「懂了 / 继续练 / 我自己来」，或本模块关键块已走完。此时才允许三块摘要：

```
怎么自己找：搜… → 打开… → 再搜谁 Create / 谁消费
使用顺序：Owner → Config → Drive →（引擎更新）→ Query
参数约束：…；空 Owner / 错角色 / Query 过早会…
```

再给 **1 个**可独立完成的小任务（标字段、改 Lab 打日志、对照一处调用等）；学生交卷后再点评，完整答案不放最前。

## 怎么找文件（带读时用，勿单独当完整回答）

- IDE：符号 → 跳转定义 / Find References。
- 引擎：`Engine/Source/Runtime/...` 或 `Engine/Plugins/...`；优先 `*.h` 注释与公开区块，需要时再跟 `.cpp` 一处赋值或调用。
- Aura 内先搜已有用法（`AbilitySystem`、`Character` 等），再对照引擎基类。
